# fablebound spec & plan — canonical locations

The v1 spec and implementation plan moved to the `m31labs/fablebound` hyphae
space on 2026-06-10. Do not author specs/plans in this repo.

| Document | Hypha URI / id | File path |
|---|---|---|
| Spec (v1, normative) | `hypha://m31labs/fablebound` · `spec.fablebound-v1` | `~/.hyphae/spaces/m31labs-fablebound/specs/fablebound-v1.md` |
| Implementation plan | `hypha://m31labs/fablebound` · `plan.fablebound-v1-implementation` | `~/.hyphae/spaces/m31labs-fablebound/plans/fablebound-v1-implementation.md` |
| Space manifest | `space.m31labs-fablebound` | `~/.hyphae/spaces/m31labs-fablebound/SPACE.md` |

Retrieve with `hypha show spec.fablebound-v1`, `hypha show
plan.fablebound-v1-implementation`, or `hypha recall "fablebound"`.

The verified default Arbiter policies are product artifacts and live in this
repo at `policy/{dispatch,toolgate}.arb` (re-verified with `arbiter check`
on 2026-06-10).
